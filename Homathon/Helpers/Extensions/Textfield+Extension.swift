//
//  Textfield+Extension.swift
//  FintechHackathon_ios
//
//  Created by يعرب المصطفى on 4/5/19.
//  Copyright © 2019 yarob. All rights reserved.
//

import Foundation
import UIKit

//REUSABLE: THIS EXTENSION IS USED TO ADD DONE BUTTON TO ANY TEXT FIELD BY SETTING THE VALUE OF ON ACCESSORY IN THE STORYBOARD IN ANY TEXT FIELD TO ON
extension UITextField{
    
    @IBInspectable var doneAccessory: Bool{
        get{
            return self.doneAccessory
        }
        set (hasDone) {
            if hasDone{
                addDoneButtonOnKeyboard()
            }
        }
    }
    
    func addDoneButtonOnKeyboard()
    {
        let doneToolbar: UIToolbar = UIToolbar(frame: CGRect.init(x: 0, y: 0, width: UIScreen.main.bounds.width, height: 50))
        doneToolbar.barStyle = .default
        
        let flexSpace = UIBarButtonItem(barButtonSystemItem: .flexibleSpace, target: nil, action: nil)
        let done: UIBarButtonItem = UIBarButtonItem(title: "Done", style: .done, target: self, action: #selector(self.doneButtonAction))
        
        let items = [flexSpace, done]
        doneToolbar.items = items
        doneToolbar.sizeToFit()
        
        self.inputAccessoryView = doneToolbar
    }
    
    @objc func doneButtonAction()
    {
        self.resignFirstResponder()
    }
}
